# WeatherApp

Full-stack weather application using React, ASP.NET Core Web API and SQLite.

## Architecture

- **CLI Project** — imports GeoNames city data into SQLite database
- **Domain** — contains core entities
- **Web API** [TODO] — serve weather data for imported cities
- **Weather Frontend** [TODO] — React app to display weather
